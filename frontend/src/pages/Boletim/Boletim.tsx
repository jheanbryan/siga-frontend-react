import { BrowserRouter } from 'react-router-dom';
import './NotFound.css';

function NotFound() {
  return(
    <BrowserRouter>
      
    </BrowserRouter>
  );
};

export default NotFound;